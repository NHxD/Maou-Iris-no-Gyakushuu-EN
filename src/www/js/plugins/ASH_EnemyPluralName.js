/*:
 * @plugindesc	correct plural localization for enemies.
 *
 * @author		ash
 *
 * @require		
 */

(function() {
	"use strict"

// TODO: it seems RPGMMV supports it already (albeit a very primitive form of it) but no idea how and where to set it up.
{
	//=============================================================================
	// Game_Enemy
	//=============================================================================

	/*
	Game_Enemy.prototype.name = function()
	{
		if (this._plural)
		{
			if (this.meta.hasOwnProperty("pluralName"))
			{
				return this.meta.pluralName
			}

			return this._letter
		}
		else
		{
			return this.originalName()
		}
	}
	*/
}

})()
